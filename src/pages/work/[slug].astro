---
/* One address per product: /site/work/<slug>. This one still answers (WEB-004). */
import { projects } from '../../data/site';
export function getStaticPaths() {
  return projects.map((p) => ({ params: { slug: p.slug } }));
}
return Astro.redirect(`/site/work/${Astro.params.slug}`, 301);
---
